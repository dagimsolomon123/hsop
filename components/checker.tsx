import React from 'react';
import {View,Text} from 'react-native'

const checker = () => {
  return (
    <View style={{alignItems: 'center',justifyContent: 'center'}}>
        <Text style={{color: 'white',fontSize: 23}}>HI</Text>
    </View>
  );
};

export default checker;
